# Jiang Wu

Personal research homepage, published with GitHub Pages.

The site is served from the root of the `main` branch. Edit `index.html` for content and `styles.css` for presentation. Original publication figures are stored in `figures/` and linked to their papers on the homepage.
